<%= simple_form_for category, url: url, method: method do |f| %>
  <%= f.error_notification %>
  <%= f.input :name, label: 'Nom de la catégorie', placeholder: 'ex : ...',
      input_html: { class: 'details-form-item static-form-item flex-row-start-center' } %>
  <%= f.input :rank, collection: category_ranks,
                     label: 'Rang',
                     selected: category.rank.nil? ? category_ranks.last : category_ranks[category.rank-1],
                     include_blank: false,
                     input_html: { class: 'details-form-item static-form-item flex-row-start-center',
                     value: category.rank } %>
  <%= f.button :submit, id: 'update-details-btn', class: 'red-btn-xl', value: button, data: { disable_with: false } %>
<% end %>
